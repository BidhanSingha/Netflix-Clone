import {view} from 'react-native'
import React from 'react'

export default function NavigationBar()
 return{
     <view style={{
         padding: 20
     }}>
         <TExt style={{fontSize: 34, fontWeight: '600', color: 'red'}}>Netflix</TExt>
         <view style={{ flexDirection:'row', justifyContent: 's|
             <text style={styles,textHeader}}}>
    </view>
 }